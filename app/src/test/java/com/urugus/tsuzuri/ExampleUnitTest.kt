package com.urugus.tsuzuri

import org.junit.Assert.assertEquals
import org.junit.Test

/** Placeholder test to verify the unit-test toolchain is wired up. */
class ExampleUnitTest {
    @Test
    fun addition_isCorrect() {
        assertEquals(4, 2 + 2)
    }
}
